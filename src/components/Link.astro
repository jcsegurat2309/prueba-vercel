---
const { title, description, url, imagen, clase, id } = Astro.props;
---

<a
  href={url}
  class="p-4 bg-white rounded-xl shadow-lg hover:shadow-2xl transform hover:-translate-y-1 transition-all duration-300 border-2 border-[#EC651B] flex flex-col sm:flex-row items-center justify-between"
  target="_blank"
  rel="noopener noreferrer"
>
  <div class="flex-1 text-center sm:text-left">
    <h2 class="text-xl font-semibold text-gray-800 mb-1">{title}</h2>
    <p class="text-sm text-gray-800">{description}</p>
  </div>
  <img src={imagen} alt="Logo" id={id} class={`h-12 w-auto mt-4 sm:mt-0 sm:ml-4 rounded ${clase}`} loading="lazy" />
</a>
